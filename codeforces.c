#include <stdio.h>

int main(){

    int cases, i, j, k, gifts, num;
    scanf("%d", &cases);
    for(i = 0; i < cases; i++)
    {
        scanf("%d", &gifts);
        for(j = 0; j < 2; j++)
        {
            int difference = 0, firstGifts[gifts], secondGifts[gifts];
            for(k = 0; k < gifts; k++)
            {
                
            }
        }
    }
    
    return 0;
}